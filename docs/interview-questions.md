"I generate a temporary .env and delete it afterwards."

They'd say:

"Nice improvement."

If you told them:

"I inject GitHub Secrets as runtime environment variables and avoid writing secrets to disk."

They'd say:

"Even better."

If you told them:

"I use AWS Secrets Manager and retrieve secrets dynamically."

They'd say:

"Production-grade.